<!--=== 币值转换 ===-->
<div class='dropdown btn-group clearfix'>
  <button class='btn dropdown-toggle' type='button' data-toggle='dropdown'>
    <%= t('nav-currency') %> <i class="fa fa-angle-down"></i>
  </button>
  <!-- 下拉式選單 -->
  <ul class='dropdown-menu dropdown-menu-right'>
    <!-- 選單列出所有幣值 -->
    <% @currencies.each do |currency| %>
      <li>
        <%= link_to setup_currency_product_path(:currency => currency.title), method: :post, :remote => true do %>
          <%= currency.symbol %> - <%= currency.title %>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>
